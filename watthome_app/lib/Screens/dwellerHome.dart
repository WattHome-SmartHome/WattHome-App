import 'package:flutter/material.dart';

class DwellerHomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Dweller Home'),
      ),
      body: Center(
        child: Text('Logout'),
      ),
    );
  }
}
